# Predicate

A Predicate is a canonical semantic relationship in the ATON ontology.

A Predicate defines a meaningful directional relationship between a source
Concept and a target Concept.

Predicate applicability is defined by explicit allowed source-to-target Concept
pairs.

A Predicate is itself an engineering knowledge artifact and therefore has an
explicit ontology type.
